spring:
  application:
    name: eureka
  cloud:
    config:
      fail-fast: true
      retry:
        maxAttempts: 200
        maxInterval: 10000
      enabled: true
      uri: http://localhost:8081
